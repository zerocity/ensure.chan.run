This directory defines the high-level concepts, business logic, and architecture of @chan.run/ensure using markdown. It is managed by [lat.md](https://www.npmjs.com/package/lat.md) — a tool that anchors source code to these definitions. Install the `lat` command with `npm i -g lat.md` and run `lat --help`.

- [[requirements]] — Quality gates, testing, and library-specific requirements (RFC 2119)
- [[capa]] — Corrective and Preventive Action log for requirement violations
